# Tech bank

One file per technology, `<slug>.md`, in the format the `tech-card` skill documents. Only the
durable ones live here — mechanisms and protocols that stay true at the next company. A vendor's
product terminology goes in that job's folder under `jds/<slug>/TECH_CARDS.md`.
